# Tasks: จองคิวตรวจสุขภาพ (Booking)
อ้างอิง: spec.md SPEC-BKG-001 Draft v2 | plan.md v1 | วันที่ 2569-09-23

สรุป: ทำทั้งหมด 13 task, 1 task ต้องรอ Open Question (Q-02)

### T-01 สร้างโครงฐานข้อมูลและ migration
- รองรับ: CON-TECH-01, DOM-PDPA-01, IF-HIS-01
- ตรวจด้วย: ไม่มี AC ตรง ๆ เป็นงานพื้นฐานของ T-01
- ไฟล์ที่แตะ: backend/app/db/models.py, backend/app/db/session.py, backend/app/db/migrations/001_init.py
- ต้องทำหลัง: ไม่มี
- เสร็จเมื่อ: migration สร้างตาราง slots, bookings, audit_logs ให้ใช้งานได้ใน PostgreSQL และ bookings เก็บเฉพาะ hn โดยไม่เก็บเลขบัตรประชาชน
- สถานะ: พร้อมทำ

### T-02 สร้าง API ค้นช่วงเวลาว่างตามแพ็กเกจ
- รองรับ: FR-BKG-01, FR-BKG-06, NFR-PERF-01
- ตรวจด้วย: AC-BKG-05
- ไฟล์ที่แตะ: backend/app/slots/router.py, backend/app/slots/service.py, backend/tests/test_slots.py
- ต้องทำหลัง: T-01
- เสร็จเมื่อ: GET /slots คืนค่าช่วงเวลาว่างภายใน 30 วันข้างหน้า พร้อม remaining และตอบสนองได้ภายใน p95 ไม่เกิน 2 วินาที เมื่อทดสอบพร้อมกัน 200 คน
- สถานะ: พร้อมทำ

### T-03 สร้าง API จองคิวพื้นฐานและตัดที่นั่ง
- รองรับ: FR-BKG-04
- ตรวจด้วย: AC-BKG-01
- ไฟล์ที่แตะ: backend/app/booking/router.py, backend/app/booking/service.py, backend/tests/test_booking_basic.py
- ต้องทำหลัง: T-02
- เสร็จเมื่อ: POST /bookings บันทึกการจอง ตัดที่นั่งของช่วงเวลานั้น และคืนหมายเลขคิวให้ผู้ใช้
- สถานะ: พร้อมทำ

### T-04 ป้องกันจองซ้ำวันเดียวกัน
- รองรับ: FR-BKG-02
- ตรวจด้วย: AC-BKG-02
- ไฟล์ที่แตะ: backend/app/booking/service.py, backend/tests/test_booking_duplicate.py
- ต้องทำหลัง: T-03
- เสร็จเมื่อ: เมื่อมีคิวที่ยังไม่ได้ใช้ในวันเดียวกัน ระบบปฏิเสธการจองใหม่และคืนหมายเลขคิวเดิมให้ผู้ใช้
- สถานะ: พร้อมทำ

### T-05 จัดการกรณีช่วงเวลาเต็มและเสนอ 3 ตัวเลือก
- รองรับ: FR-BKG-03
- ตรวจด้วย: AC-BKG-03
- ไฟล์ที่แตะ: backend/app/booking/service.py, backend/app/slots/service.py, backend/tests/test_booking_full_slot.py
- ต้องทำหลัง: T-04
- เสร็จเมื่อ: หาก slot เต็มระหว่างยืนยัน จะคืน 409 พร้อม 3 ช่วงที่ใกล้ที่สุดภายในวันเดียวกันและวันถัดไป และไม่มีการจองซ้อนเกิน 1 รายการต่อผู้รับบริการต่อวัน
- สถานะ: พร้อมทำ

### T-06 ตั้งคิวส่งข้อความยืนยันและการส่งซ้ำ
- รองรับ: FR-BKG-05, IF-NOT-01, NFR-REL-02
- ตรวจด้วย: AC-BKG-04
- ไฟล์ที่แตะ: backend/app/notify/queue.py, backend/app/booking/service.py, backend/tests/test_notify_retry.py
- ต้องทำหลัง: T-03
- เสร็จเมื่อ: ข้อความที่ไม่สำเร็จถูกบันทึกในคิวส่งซ้ำภายใน 5 นาที และการจองยังคงถูกบันทึกไว้เพื่อแสดงผลในหน้าจอหลังจาก Q-02 ชัดเจน
- สถานะ: รอ Q-02

### T-07 บันทึก audit log ทุกการเข้าถึงข้อมูลการจอง
- รองรับ: DOM-PDPA-01
- ตรวจด้วย: AC-BKG-06
- ไฟล์ที่แตะ: backend/app/audit/middleware.py, backend/app/audit/service.py, backend/tests/test_audit_log.py
- ต้องทำหลัง: T-01
- เสร็จเมื่อ: ทุกการเปิดดูข้อมูลการจองมี audit log ที่ระบุ actor_id, accessed_at และ hn โดยเก็บข้อมูลไว้ไม่น้อยกว่า 1 ปี
- สถานะ: พร้อมทำ

### T-08 ตรวจสอบผลยืนยันตัวตนก่อนเข้าถึงข้อมูลผู้รับบริการ
- รองรับ: IF-IDP-01
- ตรวจด้วย: ไม่มี AC ตรง ๆ เป็นงานพื้นฐานของ T-08
- ไฟล์ที่แตะ: backend/app/auth/idp.py, backend/app/booking/router.py
- ต้องทำหลัง: T-01
- เสร็จเมื่อ: endpoint ที่ต้องใช้ข้อมูลผู้รับบริการตรวจสอบผลยืนยันตัวตนก่อนเรียกข้อมูลและปฏิเสธเมื่อยืนยันตัวตนไม่ผ่าน
- สถานะ: พร้อมทำ

### T-09 ค้น HN จาก HIS และไม่เก็บเลขบัตรประชาชน
- รองรับ: IF-HIS-01
- ตรวจด้วย: ไม่มี AC ตรง ๆ เป็นงานพื้นฐานของ T-09
- ไฟล์ที่แตะ: backend/app/his/client.py, backend/app/booking/router.py, backend/tests/test_his_lookup.py
- ต้องทำหลัง: T-01
- เสร็จเมื่อ: ระบบค้นข้อมูลผู้รับบริการจาก HIS ด้วยเลขบัตรประชาชน แล้วอ้างอิงภายในระบบด้วย HN และไม่บันทึกเลขบัตรประชาชนในตารางการจอง
- สถานะ: พร้อมทำ

### T-10 สร้างหน้าเลือกแพ็กเกจและช่วงเวลาว่าง
- รองรับ: FR-BKG-01, FR-BKG-06
- ตรวจด้วย: ไม่มี AC ตรง ๆ เป็นงานพื้นฐานของ T-10
- ไฟล์ที่แตะ: frontend/src/pages/SlotPicker.jsx, frontend/src/api/client.js, frontend/src/App.jsx
- ต้องทำหลัง: ไม่มี
- เสร็จเมื่อ: ผู้ใช้เลือกแพ็กเกจแล้วเห็นรายการช่วงเวลาที่ว่างพร้อมจำนวนที่นั่งคงเหลือ และสามารถเปลี่ยนแพ็กเกจเพื่อเรียกช่วงเวลาใหม่
- สถานะ: พร้อมทำ

### T-11 สร้างหน้าจอยืนยันและแสดง 3 ตัวเลือกเมื่อเต็ม
- รองรับ: FR-BKG-03
- ตรวจด้วย: AC-BKG-03 (หน้าจอ)
- ไฟล์ที่แตะ: frontend/src/pages/ConfirmBooking.jsx, frontend/src/__tests__/AC-BKG-03.test.jsx
- ต้องทำหลัง: T-10
- เสร็จเมื่อ: เมื่อ API แจ้ง 409 พร้อมช่วงที่ใกล้ที่สุด 3 ตัวเลือก หน้าจอแสดงข้อความ "ช่วงเวลาเต็ม" และให้ผู้ใช้เลือกตัวเลือกที่ว่างได้
- สถานะ: พร้อมทำ

### T-12 สร้างหน้าแสดงผลการจองและลงต่อกับ API จริง
- รองรับ: FR-BKG-04, FR-BKG-05
- ตรวจด้วย: AC-BKG-01, AC-BKG-04
- ไฟล์ที่แตะ: frontend/src/pages/BookingResult.jsx, frontend/src/App.jsx, frontend/src/api/client.js
- ต้องทำหลัง: T-03, T-06, T-10, T-11
- เสร็จเมื่อ: ผู้ใช้เห็นหมายเลขคิวหลังยืนยันการจอง และยังสามารถเห็นหมายเลขคิวบนหน้าจอแม้ส่งข้อความยืนยันไม่สำเร็จ
- สถานะ: พร้อมทำ

### T-13 รอคำตอบ Q-02: กำหนดรูปแบบและการรีเซ็ตหมายเลขคิว
- รองรับ: FR-BKG-04, Q-02
- ตรวจด้วย: ไม่มี AC ตรง ๆ เนื่องจากรอคำตอบจากเจ้าหน้าที่เวชระเบียน
- ไฟล์ที่แตะ: backend/app/booking/service.py, frontend/src/pages/BookingResult.jsx
- ต้องทำหลัง: ไม่มี
- เสร็จเมื่อ: ทีมเจ้าหน้าที่เวชระเบียนตอบว่าเลขคิวรีเซ็ตรายวันหรือไหลต่อ และมีรูปแบบที่แน่ชัดก่อนเริ่มพัฒนาออกเลขคิว
- สถานะ: รอ Q-02

## ตารางตรวจความครบ AC
| AC ID | task ที่ตรวจ AC นี้ |
|---|---|
| AC-BKG-01 | T-03, T-12 |
| AC-BKG-02 | T-04 |
| AC-BKG-03 | T-05, T-11 |
| AC-BKG-04 | T-06, T-12 |
| AC-BKG-05 | T-02 |
| AC-BKG-06 | T-07 |

> หมายเหตุ: T-06 ถูกค้างไว้จนกว่าคำตอบ Q-02 จะชัดเจน เพื่อหลีกเลี่ยงการเดารูปแบบเลขคิวที่ยังไม่ยืนยันจากเจ้าหน้าที่เวชระเบียน
## ตารางตรวจความครบ Constraint
| Constraint ID | task ที่ทำให้เป็นจริง |
|---|---|
| CON-TECH-01 | T-01 |
| DOM-PDPA-01 | T-01, T-07 |
| IF-IDP-01 | T-08 |
| IF-HIS-01 | T-01, T-09 |
| IF-NOT-01 | T-06 |

## สิ่งที่ยังไม่ทำ
- Q-02: หมายเลขคิวรีเซ็ตรายวัน หรือนับต่อเนื่อง และมีรูปแบบอย่างไร (เช่น A001)? -> ต้องถามเจ้าหน้าที่เวชระเบียน
  - task ที่รออยู่: T-06, T-13
